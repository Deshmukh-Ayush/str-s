{
  "name": "str-s-mcp",
  "version": "0.1.0",
  "description": "MCP server exposing STR (SVG to React + Tailwind) as an AI agent tool.",
  "type": "module",
  "main": "./dist/index.js",
  "bin": {
    "str-s-mcp": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup",
    "start": "node dist/index.js",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.6.1",
    "str-s": "workspace:*",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@types/node": "^22.13.9",
    "tsup": "^8.4.0",
    "typescript": "^5.8.2",
    "vitest": "^3.0.8"
  }
}
